# Independent quality review — 2026-09-09

The initial review covered committed revision `e061f15e7a05f91a78a953a0c2bcf94c23cc7211`. File references below use that baseline unless an action names a later revision. Seven fresh reviewers covered security, Convex correctness, the data model, MCP/skills/hooks, installation, privacy, and documentation. A separate fresh verifier read every cited claim and reproduced the main control flows with isolated mocks.

The verified baseline has 13 MAJOR and 7 MINOR defects after deduplication. Repair and fresh repeat reviews bring the cumulative total to 15 MAJOR and 13 MINOR defects. All fifteen MAJOR defects are repaired. The fresh installation pass found zero MAJOR issues. Twelve MINOR defects are fixed. CONTRACT-04 is the only open MINOR scope. Fresh repeat reviews continue. Non-defect coverage records, duplicates, and rejected candidates are excluded from the defect totals.

The installed code-review procedure uses the full diff from the initial import, `da79997976df9cf8ab11253e1f8f4e2f4832343f`, with fresh high-reasoning agents. Its five views are instruction adherence, obvious bugs, history, prior PR comments, and code comments. There are no prior pull requests. Available agents substitute for the procedure's named models. The built-in security-review procedure uses the same diff and its high-confidence vulnerability filter. These are procedure-based reviews; no native conversational CLI run is claimed.

No reviewer touched a live deployment or user-global configuration. Root validation uses the separate development deployment and scratch homes. Production commands in the deployment guide remain owner-gated, deliberately unexecuted exceptions to command testing. Privacy review covered all tracked files, screenshots, and reachable history; no private-data disclosure was confirmed.

| severity | dimension | file:line | finding | verdict | action |
| --- | --- | --- | --- | --- | --- |
| MAJOR | Security/auth | `scripts/google-cal-auth.mjs:26`, `:35`, `:70` | **SEC-01** — Callback accepts a code with unrelated state, closes before exchange success, and omits a listening host; isolated execution confirmed acceptance. Bind loopback, validate unpredictable per-flow state, and finish successfully only after exchange/storage; PKCE is additional protection, not evidence required for this finding. | CONFIRMED | Fixed in `c5234b5` (calendar OAuth). Independent 32-case verification and the full development/OAuth suites passed. No live Google account was used. |
| MAJOR | Security/auth | `scripts/google-cal-auth.mjs:57`, `:62` | **SEC-02** — Refresh token is a subprocess argument and a failure escapes the async handler; a harmless failing Node child confirmed argv appears in the exception. Supply the value on stdin, replace command failures with fixed safe errors, and stop printing raw token responses. | CONFIRMED | Fixed in `c5234b5` (calendar OAuth). Independent 32-case verification and the full development/OAuth suites passed. No live Google account was used. |
| MAJOR | Convex correctness | `convex/queries.ts:55`, `:69` | **CVX-01** — Independently choosing then delegating/deferring a task leaves the waiting/someday row as the pick because chosen entries exclude only closed/snoozed tasks. Honor the current actionable status when reading chosen, and preserve valid chosen ordering. | CONFIRMED | Fixed in `0ee84af` (Now ordering). The cross-pass draft defect was repaired. All 29 independent ordering scenarios, TypeScript, 220 development assertions, and hook/OAuth suites passed. |
| MAJOR | Convex correctness | `convex/queries.ts:380`, `:409` | **CVX-02** — A newer terminal task in area B consumes `limit:1` before area A filtering, returning no match despite an older matching task. Apply filters before the result limit using suitable indexing/pagination; do not silently omit later matches. | CONFIRMED | Fixed in `0b8787e` (bounded queries). Independent 47-case verification, TypeScript, development push, 224 backend assertions, hosted large-record pagination, hook/OAuth/calendar suites, and real MCP tests passed. |
| MAJOR | Convex scalability/correctness | `convex/queries.ts:389`; related `:43`, `:104`, `:151`, `:182`, `:192`, `:300`, `:320`; `convex/tasks.ts:100`; `convex/checkins.ts:145`, `:169`, `:277` | **CVX-03** — `list({areaKey,limit:1})` independently read all 1,000 historical fixtures; uncapped partitions can exceed documented transaction budgets. Enforce an explicit supported per-operation capacity with overflow errors and an indexed paginated large-history route, or redesign aggregates; replacing true counts/history with silent `take` truncation is not a valid repair. | CONFIRMED | Fixed across `0b8787e` and `a4a1a85` (read/write capacities). Independent 40 mutation/scheduler, 47 query, and seven stdio checks passed, alongside TypeScript, development push, 234 backend assertions, rollback probes, and hook/OAuth/calendar/MCP suites. |
| MAJOR | Calendar correctness | `convex/meetings.ts:164`, `:172`, `:213` | **CVX-04** — Two advancing-window replacements independently produced duplicate event IDs, broke `linkPrep.unique()`, and retained old rows; Google's lower bound applies to event end, while deletion uses start. Reconcile by event ID and explicitly prune the rolling mirror while preserving matching prep state. | CONFIRMED | Fixed in `81cd7a6` (calendar mirror). Independent 36-case verification, 29 ordering cases, 213 top-level backend assertions, 14 rollback probes, hosted synthetic pagination, and hook/OAuth/calendar suites passed. |
| MAJOR | Calendar/Now ordering | `convex/meetings.ts:245`; `convex/queries.ts:69`, `:144` | **CVX-05** — A prep task promoted ten minutes before a meeting becomes urgent/today yet independently remains behind the ordinary morning choice, and a full cap can hide it. Update the explicit Now ordering with a defined precedence that still respects later deliberate demotion. | CONFIRMED | Fixed in `0ee84af` (Now ordering). The cross-pass draft defect was repaired. All 29 independent ordering scenarios, TypeScript, 220 development assertions, and hook/OAuth suites passed. |
| MAJOR | Calendar completeness | `convex/meetings.ts:68`, `:77`, `:99` | **CVX-06** — A mocked first page carrying `nextPageToken` caused immediate full-window replacement without a second event request; omitted events/prep links can disappear. Read all supported pages and fail without replacing if a supported bound prevents a complete snapshot. | CONFIRMED | Fixed in `81cd7a6` (calendar mirror). Independent 36-case verification, 29 ordering cases, 213 top-level backend assertions, 14 rollback probes, hosted synthetic pagination, and hook/OAuth/calendar suites passed. |
| MINOR | Status timestamps | `convex/checkins.ts:94`; `convex/meetings.ts:245`; `convex/tasks.ts:69` | **CVX-07** — Independent waiting→chooseToday→waiting execution retained timestamp 1 for the new wait. Route promotions through consistent transition side effects or clear waitingSince when leaving waiting. | CONFIRMED | Fixed in `0ee84af` (Now ordering). The cross-pass draft defect was repaired. All 29 independent ordering scenarios, TypeScript, 220 development assertions, and hook/OAuth suites passed. |
| MINOR | Production error contract | `convex/tasks.ts:542`; related `:57`, `:492`, `:545`–`:550`; `convex/meetings.ts:214`; `convex/ai.ts:34`, `:57`–`:63` | **CVX-08** — These intentional application failures throw plain Error; Convex documents redacting those messages in production while forwarding ConvexError data. Use ConvexError for safe expected failures, keeping unexpected/provider errors private. | CONFIRMED | Fixed in `be92b78` (shared validation). Independent 32-case validation, 16 preservation checks, TypeScript, 210 development assertions, hook/OAuth suites, and real MCP tests passed. |
| MAJOR | Background progress / bounded writes | `convex/tasks.ts:343`, `:356`, `:372`; related `convex/checkins.ts:259`, `:266` | **CVX-09** — Expired tasks are collected, patched, and accumulated into one chosen array without a bound; a budget/array overflow rolls back all progress and leaves the same backlog. Batch and continue the cron with bounded maintained ordering; bound explicit reconcile dates and total reads/writes without silently dropping completions. | CONFIRMED | Fixed in `a4a1a85` (bounded scheduled wakes). The escaped-text draft defect was repaired with Convex byte accounting. Independent 94-case verification and the full 234-assertion development suite passed, including 224 hosted rollback wake checks. |
| MINOR | MCP/backend data contract | `mcp/helm-mcp.mjs:45`, `:107`; `convex/tasks.ts:130`; `convex/validators.ts:30` | **MODEL-01** — MCP capture uses the seven-status enum while the backend's explicit capture validator has only five forward statuses. Publish a matching capture-only enum. | CONFIRMED | Fixed in `be92b78` (shared validation). Independent 32-case validation, 16 preservation checks, TypeScript, 210 development assertions, hook/OAuth suites, and real MCP tests passed. |
| MINOR | Settings validation consistency | `convex/lib/settings.ts:64`; `convex/setup.ts:22`; `.claude/skills/sweep/SKILL.md:20` | **MODEL-02** — Independent mergeSettings accepted an enabled source without a server, although setup rejects it and sweep must skip it. Share the enabled-source/nonblank-server check across configuration paths. | CONFIRMED | Fixed in `be92b78` (shared validation). Independent 32-case validation, 16 preservation checks, TypeScript, 210 development assertions, hook/OAuth suites, and real MCP tests passed. |
| MINOR | Calendar-date model | `convex/checkins.ts:54`, `:84`; `convex/lib/time.ts:23` | **MODEL-03** — Both actual writers independently persisted `2026-02-30`, while dayRange rejects it. Validate calendar dates before task transitions or check-in writes. | CONFIRMED | Fixed in `be92b78` (shared validation). Independent 32-case validation, 16 preservation checks, TypeScript, 210 development assertions, hook/OAuth suites, and real MCP tests passed. |
| MAJOR | Skill/source capture procedure | `.claude/skills/sweep/SKILL.md:22`–`:28`; matching scheduled template; `scripts/lib/sources.mjs:6` | **CONTRACT-01** — The mandatory update-watermark intersection excludes unchanged meetings booked before that watermark even when they enter today's/tomorrow's prep window. Enumerate the rolling event window independently; check stable occurrence and task state before recurring capture because completed dedupe keys intentionally create new tasks. | CONFIRMED | Fixed in `b13252b` (rolling calendar capture). Independent 15 handler, nine stdio, and ten procedural fixture scenarios passed, alongside 214 development assertions and real MCP tests. No native conversational sweep run is claimed. |
| MINOR | MCP/backend data contract | `mcp/helm-mcp.mjs:107` | **CONTRACT-02** — Same defect as MODEL-01; count only MODEL-01. The substantive mismatch is MINOR even though this report filed the duplicate as NOTE. | CONFIRMED | Counted with the matching primary finding. |
| NOTE | Contract verification coverage | `.claude/skills/brief/SKILL.md:8`; `hooks/session-end.mjs:19`, `:63` | **CONTRACT-03** — Source checks support neutral settings-first procedures and the bounded opt-in hook; the reviewer's fifteen execution cases are supporting reviewer evidence, not a claim this verifier independently reran all fifteen. No additional defect asserted. | CONFIRMED | No correction needed. |
| MAJOR | Install/configuration behavior | `scripts/setup.mjs:84`; `convex/areas.ts:15`, `:57` | **INST-01** — Snapshot returns key order, then accepting all current areas replaces stored order with array positions; the generic default changes from Work to Admin. Preserve retained order, present display order, append new rows after the maximum, and test unchanged reruns. | CONFIRMED | Fixed in `bad0426` (area ordering). Eight bundled and 19 independent scenarios, the unchanged live wizard rerun, and the full 210-assertion development suite passed. |
| MAJOR | Install/configuration behavior | `scripts/setup.mjs:84` | **ROOT-01** — Same defect as INST-01; count only INST-01. Upgrade the original MINOR candidate because the independently traced default capture category changes. | CONFIRMED | Counted with the matching primary finding. |
| MAJOR | Regression data preservation | `scripts/regression.mjs:49`, `:51`, `:78`, `:110`; `convex/testing.ts:35`–`:38`; `scripts/lib/auth-checks.mjs:83` | **ROOT-02** — Independent purge deleted an unmarked real check-in on a hardcoded date; suite purges before and after and never restores those date rows. Auth cleanup also deletes a full 1900-01-01 date. Use atomic fixture ownership/reservations and owned-row cleanup, or refuse occupied dates before any purge with concurrent-change protection. | CONFIRMED | Fixed in `a68dc85` (fixture preservation). Independent 16-case verification, TypeScript, 204 development assertions, and both hook suites passed. |
| NOTE | Browser link handling | `glass/prototype.html:1122`; `convex/validators.ts:45`; `mcp/helm-mcp.mjs:49` | **ROOT-03** — Arbitrary URL strings reach an escaped `_blank`/`noopener` link, but no script execution in the key-bearing origin was established; the exact-link Chrome probe was negative. The verifier's rerun could not launch Chrome in its sandbox. Reject the claimed XSS as unsupported, not as proof every unsafe scheme/browser is harmless. | REJECTED | No change; exploit claim unsupported. |
| NOTE | Privacy/committed examples | `LICENSE:3`; `scripts/regression.mjs:687`, `:754`; `docs/install.md:90`, `:92` | **PRIV-01** — Cited attribution and fixtures are generic/intentional; independently viewed both screenshots and parsed PNG chunks (IHDR/IDAT/IEND only, no trailing bytes). No correction is needed; the privacy reviewer's complete-history coverage remains that reviewer's evidence. | CONFIRMED | No correction needed. |
| MINOR | Documentation / installed surfaces | `docs/05-capture.md:6`; `mcp/helm-mcp.mjs:6`; `scripts/install-claude.mjs:31` | **DOC-001** — The capture guide calls Claude Tag DMs always available, but the MCP describes Tag as later work and the installer wires only local Claude Code. Describe the implemented Claude Code door and qualify or remove the unwired Tag claim. | CONFIRMED | Fixed in `50fb6aa` (documentation alignment). Independent comparison against the CLI, wizard, both sweep procedures, source presets, MCP, and backend passed. The full 234-assertion development suite and hook/OAuth/calendar fixtures passed. |
| MINOR | Quickstart prerequisite order | `README.md:29`–`:33`; `scripts/setup.mjs:48`; `scripts/lib/dev.mjs:13`, `:17` | **DOC-002** — README tells users to select cloud only after its setup command; independently inspected pinned CLI code offers existing users a local option that Helm's wizard rejects. Move cloud selection guidance before the command block; defaults/first-project behavior mean this is an edge, not a generally broken quickstart. | CONFIRMED | Fixed in `50fb6aa` (documentation alignment). Independent comparison against the CLI, wizard, both sweep procedures, source presets, MCP, and backend passed. The full 234-assertion development suite and hook/OAuth/calendar fixtures passed. |
| MAJOR | Regression configuration preservation / comment guidance | [scripts/regression.mjs:928](https://github.com/AlexCunliffe/helm/blob/e061f15e7a05f91a78a953a0c2bcf94c23cc7211/scripts/regression.mjs#L927-L929); `convex/lib/settings.ts:69`–`:74` | **CODE-001** — If initial caps are nonempty and the section fails before its caps:null reset, finally merges the saved caps into the test overrides rather than replacing them; independently reproduced `{today:7}` becoming `{today:7,waiting:3,newToday:2,focusMinutes:30}`. Restore every cap field explicitly (null for originally absent fields) in one atomic update. Persistent changes to real configuration justify MAJOR under the user's wrong-behavior/data-preservation rubric. | CONFIRMED (99/100) | Fixed in `a68dc85` (fixture preservation). Independent 16-case verification, TypeScript, 204 development assertions, and both hook suites passed. |
| MAJOR | Regression calendar preservation | `scripts/regression.mjs:686`, `:689`, `:737` at `0ee84af`; `convex/meetings.ts:164` | **ROOT-04** — The calendar test replaces real meeting rows, then restores a projection that omits prep links and promotion stamps. Independent execution confirmed changed row IDs and lost prep state. Run the real helpers inside a transaction that always rolls back. | CONFIRMED | Fixed in `81cd7a6` (calendar mirror). The always-rollback transaction reached all 14 assertions on development. Independent reproduction confirmed the original loss and the repaired rollback behavior. |
| MINOR | Documentation / sweep contract | `docs/05-capture.md:8`, `:11`, `:16`, `:31`; `docs/07-ai-integration.md:30`; `docs/11-decisions.md:7` at `0b8787e` | **DOC-003** — Design references still require watermark-only reads for every source and omit the calendar completion-preservation exception, contradicting the repaired full-window procedure. | CONFIRMED | Fixed across `50fb6aa` and `8a24395`. The residual source-adapter reference is removed. Independent comparison with docs05 and both source procedures passed. |

| MAJOR | Installation / development isolation | `scripts/lib/dev.mjs:11`, `:22`, `:24`; `scripts/regression.mjs:35` at `50fb6aa` | **INST-02** — The first regex selector can differ from the CLI's effective dotenv selection, and deployment credentials or self-hosted fields can override it. | CONFIRMED (100/100) | Fixed in `6380018`. Exact dotenv parsing, rejected conflicting fields, stripped ambient aliases, and per-call validation bind all administration paths. Independent 85-case verification, 234 backend assertions, hook/OAuth/calendar/MCP, wizard, and scratch-home installer checks passed. No production invocation occurred. |
| MINOR | HTTP output capacity | `convex/http.ts:34`, `:61`; `convex/queries.ts:200` at `50fb6aa` | **CVX-10** — Legal escaped task text fits the Convex value/read budgets but repeated HTTP views can exceed the 20 MiB encoded response limit. | CONFIRMED (99/100) | Fixed in `8a24395`. Native JSON output is bounded at 8 MiB; overflow returns a small 413 error. Independent 100 boundary cases, seven HTTP handler cases, three hosted-probe assertions, and the 237-assertion development suite passed. |
| MINOR | Count contract | `convex/queries.ts:157`, `:169`, `:216`; `docs/configure.md:74` at `50fb6aa` | **CVX-11** — Wins/ageing counts change with display caps while waiting/upcoming counts report eligible totals. This is a contract ambiguity. | CONFIRMED (95/100) | Fixed in `8a24395`. Counts precede display slicing; selected-today semantics are documented. Independent actual-query fixtures passed across caps 100/5/1/0, alongside hosted cap invariance checks and the full suite. |
| MINOR | Regression state preservation | `scripts/regression.mjs:313`, `:355` at `50fb6aa` | **ROOT-05** — The live wake test creates an unowned current-day morning row when none existed; its final upsert leaves an empty row. | CONFIRMED (100/100) | Fixed in `4ecdc35`. D2 uses the always-rollback wake probe. Independent absent/present/failure rollback cases, 40 mutation cases, 230 hosted probe checks, and the full 228-assertion suite passed. |
| MINOR | MCP / reconcile procedure | `.claude/skills/reconcile/SKILL.md:17`; matching scheduled variant at `50fb6aa` | **CONTRACT-04** — The procedure promises carried tasks without reading the saved carried set; brief is capped and can include unrelated backlog. | CONFIRMED (100/100) | Open: read the saved evening check-in and hydrate carried IDs in both variants. |

| MINOR | Installation / URL normalization | `scripts/lib/dev.mjs:24` at `6380018` | **INST-03** — An accepted trailing slash creates `//api/query`, `//brief`, and `//glass` paths. The actual HTTP router does not match the surface route. | CONFIRMED (100/100) | Fixed in this commit. Canonical origins produce correct API/surface paths. Independent standard/regional host cases, 53 target fixtures, the full 228-assertion backend suite, and MCP tests passed. |

The diff review found CODE-001 at confidence 99/100 after independent verification. The other code-review views found no qualifying new issue. The security-diff reviewer found no new HIGH or MEDIUM candidate above 80% confidence; no per-finding false-positive review was needed. The inherited OAuth findings remain in the whole-codebase table above.

A fresh final eligibility check confirmed the requested full-diff review remains applicable. Findings are recorded here; no external review comment was posted.
