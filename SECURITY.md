# Security reports

Use the repository's private vulnerability reporting form, when available. Do not put credentials or private task content in public issues.

This reference implementation has no support agreement or guaranteed response time. The full threat model is scheduled for `docs/security.md`.

Keep each deployment's credentials separate. Use a development deployment for tests. Do not commit local configuration or secrets.
