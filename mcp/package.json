{
  "name": "helm-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Helm MCP server — exposes the Helm brain (Convex) as stdio MCP tools for Claude Code / Claude Tag",
  "bin": { "helm-mcp": "./helm-mcp.mjs" },
  "scripts": {
    "test": "node test-client.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "convex": "^1.42.0",
    "zod": "^3.23.8"
  }
}
